### VPC 
This is a module to create VPC in aws.
- User have to pass cidr.
- User can create multiple subnets.
- AZs
- User can create private and public subnets(IGW will be created and routing table ) (dfault private).
