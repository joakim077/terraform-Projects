### VPC 
This is a module that creates VPC in AWS.
- The user has to pass CIDR.
- User can create multiple subnets in different AZs.
- User can create private and public subnets(IGW will be created and routing table ) (default private).
